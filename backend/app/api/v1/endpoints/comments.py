"""Comments endpoints."""


from fastapi import APIRouter

router = APIRouter(prefix="/comments", tags=["comments"])

# Currently acts as a placeholder as the instruction says:
# "shared comment utilities if needed, otherwise leave as placeholder registering the router"
